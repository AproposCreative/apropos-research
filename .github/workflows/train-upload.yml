name: Train and Upload Embeddings

on:
  workflow_dispatch: {}
  push:
    branches: [ main ]
  schedule:
    - cron: '0 3 * * *' # daily at 03:00 UTC

jobs:
  train-upload:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Use Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: 'package-lock.json'

      - name: Install dependencies
        run: npm ci

      - name: Debug environment
        run: |
          chmod +x scripts/debug-env.sh
          ./scripts/debug-env.sh

      - name: Verify environment variables
        run: |
          echo "Checking environment variables from .env.local..."
          source .env.local
          if [ -z "$OPENAI_API_KEY" ]; then
            echo "❌ OPENAI_API_KEY is missing from .env.local"
            exit 1
          fi
          if [ -z "$FIREBASE_ADMIN_PROJECT_ID" ]; then
            echo "❌ FIREBASE_ADMIN_PROJECT_ID is missing from .env.local"
            exit 1
          fi
          if [ -z "$FIREBASE_ADMIN_CLIENT_EMAIL" ]; then
            echo "❌ FIREBASE_ADMIN_CLIENT_EMAIL is missing from .env.local"
            exit 1
          fi
          if [ -z "$FIREBASE_ADMIN_PRIVATE_KEY" ]; then
            echo "❌ FIREBASE_ADMIN_PRIVATE_KEY is missing from .env.local"
            exit 1
          fi
          echo "✅ All required environment variables are set"

      - name: Train embeddings
        run: |
          echo "🔧 Loading environment variables..."
          source .env.local
          echo "✅ Environment variables loaded"
          echo "🚀 Starting embeddings training..."
          npx tsx scripts/train-embeddings-clean.ts

      - name: Upload embeddings to Firebase Storage
        run: |
          echo "🔧 Loading environment variables for upload..."
          source .env.local
          echo "✅ Environment variables loaded"
          echo "📤 Uploading embeddings to Firebase Storage..."
          npx tsx scripts/upload-to-storage.ts data/articles-embeddings.json apropos-config/embeddings/articles-embeddings.json

      - name: Upload prompt to Firebase Storage
        run: |
          echo "🔧 Loading environment variables for prompt upload..."
          source .env.local
          echo "✅ Environment variables loaded"
          echo "📤 Uploading prompt to Firebase Storage..."
          npx tsx scripts/upload-to-storage.ts prompts/apropos_writer.prompt apropos-config/prompts/apropos_writer.prompt


