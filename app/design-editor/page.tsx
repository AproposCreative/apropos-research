'use client';

import DesignEditorView from './DesignEditorView';

/** Standalone side: Design Editor med egen side (fx ved direkte link til /design-editor). */
export default function DesignEditorPage() {
  return <DesignEditorView />;
}
